---
import Video from "../assets/videos/hero.webm";
---

<section class="hero">
  <video autoplay muted loop class="hero-video">
    <source src={Video} type="video/mp4" />
    Tu navegador no soporta el video.
  </video>
  <div class="hero-text">
    <h1>Concesionaria Premium Quality</h1>
    <p>Tu auto ideal te está esperando.</p>
  </div>
  <iconify-icon class="icon-scroll" icon="mingcute:down-fill"></iconify-icon>
</section>

<style>
  .hero {
    height: 100vh;
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    margin-bottom: 5em;
  }

  .hero-video {
    position: absolute;
    width: 100%;
    height: 100%;
    object-fit: cover;
    box-shadow: var(--box-shadow);
  }

  .hero .hero-text {
    position: relative;
    display: flex;
    align-items: center;
    flex-direction: column;
    color: white;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
  }

  .hero .hero-text h1 {
    font-size: 3em;
    margin: 0;
  }

  .hero .icon-scroll {
    position: absolute;
    bottom: 0;
    color: white;
    animation: bounce 2s infinite;
    font-size: 2em;
  }

  @keyframes bounce {
    0%,
    20%,
    50%,
    80%,
    100% {
      transform: translateY(0);
    }

    40% {
      transform: translateY(-20px);
    }

    60% {
      transform: translateY(-10px);
    }
  }
  @media (max-width: 450px) {
    .hero .hero-text h1 {
      font-size: 2em;
    }
  }
</style>
