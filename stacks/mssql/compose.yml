services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2019-latest
    container_name: sqlserver
    env_file: .env
    ports:
      - "1433:1433"
      - "11433:1433"
    volumes:
      - sqlserverdata:/var/opt/mssql
    restart: no

volumes:
  sqlserverdata:
    driver: local
